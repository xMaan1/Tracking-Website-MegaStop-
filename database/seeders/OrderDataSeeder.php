<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use App\Models\Order;
use Carbon\Carbon;
use Illuminate\Support\Str;

class OrderDataSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        // No orders added here - they will be added manually from the website
        $orders = [
            // Orders will be added manually through the website interface
        ];
        
        // No orders are created in the seeder as they will be added manually
    }
}